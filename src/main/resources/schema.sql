
CREATE TABLE students (
  empNumber Int PRIMARY KEY,
  name VARCHAR(250) NOT NULL,
  role VARCHAR(250) NOT NULL
);

CREATE TABLE employees (
  empNumber Numeric PRIMARY KEY,
  name VARCHAR(250) NOT NULL,
  role VARCHAR(250) NOT NULL
);
